====================================================================================================
表名：uav_landing_points
备注：uav起降点基础信息表：存储uav的经纬度传感器数据
生成时间：2026-02-06 23:14:42
数据记录数：4
====================================================================================================

【表结构】
表结构中，有以下字段名，它们的属性如下：
1. 字段名：id，类型为int，是否可以为空为否，是否是主键为是，默认值为NULL，额外属性为auto_increment。
2. 字段名：name，类型为varchar(50)，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
3. 字段名：location_x，类型为decimal(12,6)，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
4. 字段名：location_y，类型为decimal(12,6)，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
5. 字段名：description，类型为varchar(200)，是否可以为空为是，是否是主键为否，默认值为NULL，额外属性为为空。
6. 字段名：created_at，类型为datetime，是否可以为空为否，是否是主键为否，默认值为CURRENT_TIMESTAMP，额外属性为DEFAULT_GENERATED。

----------------------------------------------------------------------------------------------------

【表数据】
表数据记录为：
第1个数据：id是1,name是中山大学深圳校区起降点,location_x是113.904500,location_y是22.770500,description是位于校区北门广场，适合接收医疗物资,created_at是2026-02-06 21:26:01。
第2个数据：id是2,name是深圳北站起降点,location_x是114.029000,location_y是22.609000,description是交通枢纽中转站,created_at是2026-02-06 21:26:01。
第3个数据：id是3,name是光明城站起降点,location_x是113.945000,location_y是22.735000,description是光明区重要节点,created_at是2026-02-06 21:26:01。
第4个数据：id是4,name是宝安机场物流起降点,location_x是113.811000,location_y是22.639000,description是航空物流接驳点,created_at是2026-02-06 21:26:01。