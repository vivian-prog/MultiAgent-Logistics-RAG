# RAG 与 GraphRAG 模式选择说明

## 模式介绍

| 模式 | 说明 |
|------|------|
| **RAG** | 普通向量检索 RAG，基于语义相似度匹配文档片段 |
| **GraphRAG** | 知识图谱 RAG，基于实体关系推理获取相关知识 |

## 使用方法

### 命令行参数

```bash
# 启用 GraphRAG 模式 (知识图谱检索) - 默认
python3 main.py --rag-type graphrag

# 启用普通 RAG 模式 (向量检索)
python3 main.py --rag-type rag

# 禁用 RAG，仅使用 LLM
python3 main.py --no-rag

# 组合使用：指定 RAG 类型 + 自定义提示词
python3 main.py --rag-type graphrag --prompt "把生鲜送到深圳湾"
python3 main.py --rag-type rag --prompt "把锂电池从龙岗仓库运到光明区"
```

### 完整参数列表

| 参数 | 说明 |
|------|------|
| `--rag` | 启用 RAG 检索 (默认) |
| `--no-rag` | 禁用 RAG 检索，仅使用 LLM |
| `--rag-type rag` | 使用普通 RAG (向量检索) |
| `--rag-type graphrag` | 使用 GraphRAG (知识图谱) |
| `--prompt "xxx"` | 自定义用户提示词 |

### 全局配置

在 `main.py` 中可修改默认值：

```python
# 第13行
RAG_TYPE = "graphrag"  # "rag" 或 "graphrag"
```

## 模式对比

| 指标 | RAG (向量检索) | GraphRAG (知识图谱) |
|------|---------------|-------------------|
| 检索方式 | 语义相似度匹配 | 实体关系推理 |
| 输出格式 | 文档片段列表 | 实体 + 关系 + 推理结论 |
| 适用场景 | 文档问答、信息检索 | 复杂推理、知识关联 |
| 响应特点 | 直接返回相关文档 | 提供推理链和结论 |

## 运行示例

### GraphRAG 模式

```
============================================================
  多智能体物流调度系统 - GraphRAG (知识图谱) + LLM 模式
============================================================

>>> 阶段1: 指令生成
==================================================
🔍 检索模式: GraphRAG (知识图谱)
==================================================

正在执行 GRAPHRAG 搜索...
GRAPHRAG 搜索结果:
【知识图谱检索结果】
以下是基于实体关系推理的相关知识：

【相关实体】
- 干粉灭火器 (货物): category:消防器材, hazard_level:低
- 深圳北仓库 (仓库): capacity:5000件, type:综合仓库
- 光明天安云谷起降点 (起降点): uav_capacity:50

【实体关系】
- 干粉灭火器 --[存储于]--> 深圳北仓库
- 深圳北仓库 --[最近起降点]--> 光明天安云谷起降点
- robot_001 --[隶属于]--> 深圳北仓库

【推理结论】
基于知识图谱分析：
- 干粉灭火器存储于深圳北仓库(ID:1)
- 深圳北仓库最近的无人机起降点是光明天安云谷起降点
...
```

### RAG 模式

```
============================================================
  多智能体物流调度系统 - RAG (向量检索) + LLM 模式
============================================================

>>> 阶段1: 指令生成
==================================================
🔍 检索模式: RAG (向量检索)
==================================================

正在执行 RAG 搜索...
RAG 搜索结果:
【向量检索结果】
以下是基于语义相似度匹配的相关文档片段：

【仓库信息】
- 深圳北仓库(ID:1): 位于(22.603, 114.029), 类型: 综合仓库
...
```

### 仅 LLM 模式

```
============================================================
  多智能体物流调度系统 - 仅LLM 模式
============================================================

>>> 阶段1: 指令生成
==================================================
🔍 检索模式: 禁用 (仅LLM)
==================================================

跳过 RAG 检索，直接调用 LLM...
```
