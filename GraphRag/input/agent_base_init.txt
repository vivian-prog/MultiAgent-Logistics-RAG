====================================================================================================
表名：agent_base
备注：Agent基础信息表：存储各类Agent的基础属性，如Agent名称、类型、状态等
生成时间：2026-02-06 21:48:06
数据记录数：10
====================================================================================================

【表结构】
表结构中，有以下字段名，它们的属性如下：
1. 字段名：agent_id，类型为varchar(20)，是否可以为空为否，是否是主键为是，默认值为NULL，额外属性为为空。
2. 字段名：agent_type，类型为tinyint，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
3. 字段名：warehouse_id，类型为int，是否可以为空为是，是否是主键为否，默认值为NULL，额外属性为为空。
4. 字段名：max_load，类型为decimal(8,2)，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
5. 字段名：max_speed，类型为decimal(8,2)，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
6. 字段名：battery_capacity，类型为int，是否可以为空为否，是否是主键为否，默认值为NULL，额外属性为为空。
7. 字段名：status，类型为tinyint，是否可以为空为否，是否是主键为否，默认值为0，额外属性为为空。
8. 字段名：last_maintain_time，类型为datetime，是否可以为空为是，是否是主键为否，默认值为NULL，额外属性为为空。

----------------------------------------------------------------------------------------------------

【表数据】
表数据记录为：
第1个数据：agent_id是GROUND-001,agent_type是2,warehouse_id是2,max_load是50.00,max_speed是2.00,battery_capacity是20000,status是2,last_maintain_time是2025-11-19 20:00:47。
第2个数据：agent_id是GROUND-002,agent_type是2,warehouse_id是5,max_load是60.00,max_speed是2.50,battery_capacity是25000,status是1,last_maintain_time是2025-12-04 20:00:47。
第3个数据：agent_id是GROUND-003,agent_type是2,warehouse_id是8,max_load是55.00,max_speed是2.20,battery_capacity是22000,status是3,last_maintain_time是2025-11-16 20:00:47。
第4个数据：agent_id是UAV-001,agent_type是1,warehouse_id是NULL,max_load是5.00,max_speed是15.00,battery_capacity是10000,status是1,last_maintain_time是2025-11-14 20:00:47。
第5个数据：agent_id是UAV-002,agent_type是1,warehouse_id是4,max_load是8.00,max_speed是18.00,battery_capacity是12000,status是3,last_maintain_time是2025-11-29 20:00:47。
第6个数据：agent_id是UAV-003,agent_type是1,warehouse_id是7,max_load是6.50,max_speed是16.00,battery_capacity是11000,status是1,last_maintain_time是2025-11-14 20:00:47。
第7个数据：agent_id是UAV-004,agent_type是1,warehouse_id是10,max_load是7.20,max_speed是17.00,battery_capacity是11500,status是0,last_maintain_time是2025-11-26 20:00:47。
第8个数据：agent_id是WARE-001,agent_type是3,warehouse_id是3,max_load是20.00,max_speed是1.00,battery_capacity是15000,status是0,last_maintain_time是2025-11-24 20:00:47。
第9个数据：agent_id是WARE-002,agent_type是3,warehouse_id是6,max_load是25.00,max_speed是1.20,battery_capacity是18000,status是2,last_maintain_time是2025-12-09 20:00:47。
第10个数据：agent_id是WARE-003,agent_type是3,warehouse_id是9,max_load是18.00,max_speed是0.80,battery_capacity是14000,status是1,last_maintain_time是2025-11-22 20:00:47。