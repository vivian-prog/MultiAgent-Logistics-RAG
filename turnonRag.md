# RAG 模式使用说明

## 使用方法

### RAG 模式对比实验

支持启用/禁用 RAG 检索，用于对比实验：

```bash
# 启用 RAG 模式 (默认) - RAG检索 + LLM生成
python3 main.py --rag

# 禁用 RAG 模式 - 仅 LLM 生成
python3 main.py --no-rag

# 使用自定义提示词
python3 main.py --prompt "把生鲜送到深圳湾"
python3 main.py --no-rag --prompt "把锂电池从龙岗仓库运到光明区"
```

### 命令行参数

| 参数 | 说明 |
|------|------|
| `--rag` | 启用 RAG 检索 (默认) |
| `--no-rag` | 禁用 RAG 检索，仅使用 LLM |
| `--prompt "xxx"` | 自定义用户提示词 |
| `-h, --help` | 显示帮助信息 |

### 全局配置

在 `main.py` 第12行可修改默认值：

```python
ENABLE_RAG = True  # True=启用RAG, False=禁用RAG
```

## 性能对比

| 指标 | RAG+LLM | 仅LLM |
|------|---------|-------|
| RAG搜索耗时 | ~1.5s | 跳过 |
| LLM推理耗时 | ~0.02s | ~0.5s |
| 知识库支持 | ✓ | ✗ |
| 指令准确性 | 高 (基于真实数据) | 中 (使用默认值) |

## 运行示例

### RAG+LLM 模式

```
============================================================
  多智能体物流调度系统 - RAG+LLM 模式
============================================================

>>> 阶段1: 指令生成
🔍 RAG 模式: 启用 (RAG+LLM)

RAG搜索结果:
【仓库信息】
- 深圳北仓库(ID:1): 位于(22.603, 114.029)
...

[性能统计] RAG搜索耗时: 1.49s | LLM推理耗时: 0.02s
```

### 仅LLM 模式

```
============================================================
  多智能体物流调度系统 - 仅LLM 模式
============================================================

>>> 阶段1: 指令生成
🔍 RAG 模式: 禁用 (仅LLM)

跳过 RAG 检索，直接调用 LLM...

[性能统计] LLM推理耗时: 0.50s
```
